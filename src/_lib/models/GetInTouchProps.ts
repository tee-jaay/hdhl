import { IconProp } from "@fortawesome/fontawesome-svg-core";

export default interface GetInTouchItemProps {
    link: string,
    icon: IconProp,
    color: string,
    text: string,
    count: string,
}